// This component is deprecated and has been replaced by `status-badge.tsx`.
// It is kept here for reference during the transition but will be removed.
// The new system uses a `status` of 'pending', 'approved', or 'rejected'
// instead of the qualitative `BadgeStatus`.
